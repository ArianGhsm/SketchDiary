# قرارداد UX اینلاین (الزامی)

این پروژه باید **Inline-First** باقی بماند.

## قواعد قطعی

1. هر قابلیت جدید باید از callback اینلاین شروع شود.
2. ناوبری command-centric ممنوع است.
3. ورودی متنی فقط برای Data Entry مجاز است:
- شماره دانشجویی
- عنوان لیست
- متن اطلاعیه
- لیست نمرات
4. هر پیام باید حداقل یک مسیر بازگشت داشته باشد:
- `🏠 بازگشت به منو`
- `🛑 لغو`
- یا بازگشت به پنل مرتبط
5. قبل از احراز هویت، امکانات اصلی نمایش داده نشود (Auth-First).
6. سطح command حداقلی بماند:
- `/start`
- `/cancel`

## محل پیاده‌سازی در ساختار جدید

- کیبوردها: `bot/ui/keyboards.py`
- متن‌ها: `bot/ui/texts.py`
- جریان‌ها: `bot/handlers.py`
- ثبت مسیرها: `bot/application.py`

## چک‌لیست قبل از Merge

1. callback جدید در `app_callbacks.py` تعریف شده؟
2. مسیر از اینلاین شروع می‌شود؟
3. مسیر data-entry state مناسب دارد؟
4. fallback/back/cancel کامل است؟
5. Auth-First حفظ شده؟
6. `README.md` همزمان آپدیت شده؟
