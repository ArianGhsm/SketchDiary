# سیاست UX اینلاین

## اصل پایه

این مخزن باید **Inline-First** بماند.

یعنی:

- شروع قابلیت‌ها با callback اینلاین
- استفاده از متن آزاد فقط برای مرحله‌های data-entry
- وجود مسیر بازگشت یا لغو در تمام جریان‌ها

## قوانین اجباری

1. ناوبری مبتنی بر command پیاده‌سازی نشود.
2. برای قابلیت جدید، entry point باید دکمه اینلاین باشد.
3. ورودی متنی فقط برای این جنس مرحله‌ها مجاز است:
   - شماره دانشجویی
   - معرفی کوتاه
   - عنوان فرم
   - توضیح فرم
   - مهلت فرم
   - عنوان درس
   - لیست نمره
   - متن اطلاعیه
4. همه‌ی جریان‌ها باید دکمه `لغو` یا `بازگشت` داشته باشند.
5. قابلیت‌های دانشجویی قبل از تایید هویت کامل، باز نشوند.

## استاندارد متن

- متن باید فارسی، محترمانه و کوتاه باشد.
- داده‌های قابل‌کپی با `monospace` نمایش داده شوند.
- برای بلوک‌های اطلاعاتی از چیدمان عمودی استفاده شود.
- متن‌های راهنمای عمومی و شلوغ در منوی اصلی اضافه نشوند.

## استاندارد دکمه

- `✅` برای تایید و ادامه
- `❌` برای لغو، رد یا انصراف
- `↩️` برای بازگشت
- دکمه‌ها باید بر اساس عمل واقعی نام‌گذاری شوند، نه برچسب‌های مبهم

## استاندارد زمان

اگر تاریخ یا مهلتی به کاربر نشان داده می‌شود:

- باید جلالی باشد
- باید تهران‌محور باشد
- ترجیحا با `<tg-time ...>` رندر شود

## چک‌لیست قبل از تحویل

- callback جدید در `app_callbacks.py` تعریف شده؟
- متن جدید در `bot/ui/texts.py` آمده؟
- کیبورد جدید در `bot/ui/keyboards.py` آمده؟
- state جدید فقط برای data-entry تعریف شده؟
- Auth-First حفظ شده؟
- README به‌روزرسانی شده؟
