# سیاست UX اینلاین

## اصل پایه

این پروژه باید **Inline-First** بماند.

## قوانین اجباری

1. feature جدید با callback اینلاین شروع شود.
2. command جدید برای ناوبری اضافه نشود.
3. ورودی متنی فقط برای data-entry استفاده شود.
4. هر flow دکمه `لغو` یا `بازگشت` داشته باشد.
5. Auth-First شکسته نشود.

## استاندارد متن

- فارسی
- کوتاه
- حرفه‌ای
- قابل‌اسکن
- بدون help pageهای شلوغ

## استاندارد نمایش داده

- شماره دانشجویی: monospace
- آیدی عددی تلگرام: monospace
- یوزرنیم در صورت نیاز: monospace
- تاریخ مهم: جلالی + تهران + `tg-time`

## استاندارد دکمه

- `✅` برای تایید
- `❌` برای لغو یا رد
- `↩️` برای بازگشت
- برچسب دکمه باید شفاف و عملیاتی باشد

## زمان

نمایش زمان خام UTC یا ISO در UI ممنوع است.

## چک‌لیست توسعه

- callback در `app_callbacks.py`
- متن در `bot/ui/texts.py`
- کیبورد در `bot/ui/keyboards.py`
- state فقط برای data-entry
- README آپدیت‌شده
