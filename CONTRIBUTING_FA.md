# راهنمای مشارکت (الزامی)

این فایل برای همه ادیتورها (انسان یا ربات) است.

## قانون طلایی

هر بار کد تغییر می‌کند، باید مستندات هم تغییر کند.

حداقل مستندات اجباری:
- `README.md`

مستندات تکمیلی (در صورت نیاز):
- `ARCHITECTURE_FA.md`
- `INLINE_UX_POLICY_FA.md`
- `AGENTS.md`

## Definition of Done

یک تغییر زمانی «کامل» است که:
1. کد اجرا شود و خطای سینتکس نداشته باشد.
2. مسیر UX اینلاین حفظ شده باشد.
3. اگر رفتار/قابلیت/فرمت ورودی/نقش‌ها تغییر کرده، `README.md` آپدیت شده باشد.
4. اگر import/وابستگی جدید اضافه شده، `requirements.txt` آپدیت شده باشد.
5. اگر قرارداد معماری یا UX تغییر کرده، فایل قرارداد مربوطه هم آپدیت شده باشد.

## چک‌لیست قبل از تحویل

1. آیا قابلیت جدید از دکمه اینلاین شروع می‌شود؟
2. آیا برای مسیر جدید مسیر بازگشت/لغو اینلاین وجود دارد؟
3. آیا callback جدید در `app_callbacks.py` تعریف شده؟
4. آیا README شامل توضیح قابلیت جدید، نحوه استفاده و محدودیت‌هاست؟
5. آیا در صورت نیاز راه‌اندازی/تنظیمات جدید در README اضافه شده؟
6. آیا در صورت تغییر وابستگی‌ها، requirements آپدیت شده؟
7. آیا داده‌های عملیاتی همچنان در پوشه `data/` متمرکز مانده‌اند؟

## گیت‌هوک پیشنهادی (نیمه‌اجباری)

برای جلوگیری از فراموشی مستندات، این پروژه یک pre-commit hook دارد:

```bash
git config core.hooksPath .githooks
```

اسکریپت مرتبط:
- `tools/check_readme_sync.py`

## سیاست رد تغییر

تغییری که کد را عوض کند اما README را به‌روزرسانی نکند، باید ناقص تلقی شود و مرج نشود.
